import { StyleSheet } from "react-native"

import { space, colors } from "../../styles"

export const styles = StyleSheet.create({
	title: {
		height: space.xxxlrg + space.xlrg
	},
	sub: { marginBottom: space.med, marginTop: space.small }

})
